I want to build Authentication & Authorization System (JWT + OAuth) following the requirements:

- Tech Stack: Python3, FastApi, MongoDB, JWT
- Apply TDD to develop the system, ensure this coverage is above 90%
- Use Docker to containerize the applicat
